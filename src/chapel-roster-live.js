import { createClient } from '@supabase/supabase-js';
const supabase=createClient('https://jpfnhwolttfisawfthbf.supabase.co','sb_publishable_vsHZotBHUEePBvunVgTWWQ_fIImlhYY',{auth:{persistSession:true,autoRefreshToken:true,storageKey:'lancers-chapel-session'}});
function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
async function render(){const card=document.querySelector('.feature-event');if(!card)return;const {data,error}=await supabase.functions.invoke('chapel-roster',{body:{}});if(error||data?.error)return;const rows=data?.rows||[],yes=rows.filter(r=>r.attending===true),no=rows.filter(r=>r.attending===false);card.querySelectorAll('#chapel-rsvp-roster,.core-rsvp,.core-rsvp-roster,.live-rsvp-roster').forEach(n=>n.remove());let box=document.createElement('div');box.id='chapel-rsvp-roster-live';box.className='live-rsvp-roster';box.innerHTML=`<h3>Chapel RSVP</h3><div class="rsvp-summary"><b>${yes.length} coming</b><span>${no.length} can't make it</span></div><div class="rsvp-names">${yes.length?yes.map(r=>`<span>✓ ${esc(r.display_name)}${r.jersey_number?` #${esc(r.jersey_number)}`:''}</span>`).join(''):'<span>No RSVPs yet.</span>'}</div>${no.length?`<details><summary>Can't make it (${no.length})</summary>${no.map(r=>`<span>${esc(r.display_name)}</span>`).join('')}</details>`:''}`;card.appendChild(box)}
let running=false;async function run(){if(running)return;running=true;try{await render()}finally{running=false}}
new MutationObserver(()=>setTimeout(run,120)).observe(document.documentElement,{childList:true,subtree:true});document.addEventListener('click',e=>{if(e.target.closest?.('.rsvp-buttons'))setTimeout(run,500)},true);window.addEventListener('load',run);setInterval(run,1200);